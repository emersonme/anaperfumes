import styled from 'styled-components'
import {BorderlessButton, RectButton} from 'react-native-gesture-handler'

export const BordelessTouch = styled(BorderlessButton)``

export const Touch = styled(RectButton)``
